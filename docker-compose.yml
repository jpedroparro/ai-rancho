version: '3.8'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: file:/app/data/prod.db
      NEXTAUTH_SECRET: irancho-super-secret-change-in-production
      NEXTAUTH_URL: http://localhost:3000
    volumes:
      - ./data:/app/data
